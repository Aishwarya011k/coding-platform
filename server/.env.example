# Example .env for server
# SMTP (Gmail with App Password recommended)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=oslcodingplatform@gmail.com
# NOTE: Do NOT store real secrets in this file when committing. Replace values locally only.
SMTP_PASS=
SMTP_FROM="OSL Coding Platform <oslcodingplatform@gmail.com>"
# Optional: Use SendGrid API instead of SMTP. If set, SendGrid will be preferred.
SENDGRID_API_KEY=
# Optional: Attempt direct MX delivery (deliver directly to recipient mail servers).
# Not recommended for production — deliverability will be poor without SPF/DKIM/PTR, and many hosts block direct SMTP.
SEND_DIRECT=false

# JWT secret
JWT_SECRET=replace-with-a-secure-random-value

# Server port (optional)
PORT=3001
